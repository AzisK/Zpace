# Zpace Configuration File
# Copy this file to ~/.zpace.toml to customize categories and extensions.
#
# ===== FILE CATEGORIES =====
# Structure:
#   [categories.<CategoryName>]
#   extensions = [".ext1", ".ext2"]  # Replace all extensions for this category
#   add = [".ext3"]                  # Add extensions to existing category
#   remove = [".ext4"]               # Remove extensions from existing category

# Example: Add a Fonts category (not included by default)
[categories.Fonts]
extensions = [".ttf", ".otf", ".woff", ".woff2", ".eot"]

# Example: Add SQL files to the Code category
# [categories.Code]
# add = [".sql", ".graphql"]

# Example: Remove markdown from Documents (if you prefer it in Code)
# [categories.Documents]
# remove = [".md"]

# Example: Create a completely custom category
# [categories."Design Files"]
# extensions = [".sketch", ".fig", ".xd", ".psd", ".ai"]

# ===== DIRECTORY CATEGORIES =====
# Structure:
#   [directories.<CategoryName>]
#   dirs = ["dir1", "dir2"]  # Replace all directory names for this category
#   add = ["dir3"]           # Add directory names to existing category
#   remove = ["dir4"]        # Remove directory names from existing category

# Example: Add custom virtual environment names
# [directories."Virtual Environments"]
# add = ["myenv", ".myenv"]

# Example: Add more build artifact directories
# [directories."Build Artifacts"]
# add = ["_build", ".output"]

# Example: Create a custom directory category
# [directories."Data Folders"]
# dirs = ["datasets", "raw_data", "processed"]
